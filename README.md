# React + Babel + Webpack + npm Boilerplate

A sample environment to get you started with React, ES6, Webpack and Babel.

## Usage:

```
npm install # Install dependencies
npm run bundle # Bundle into bundle.js
npm run watch # Watch
```